# Sanitized emulator-log fixtures

Every fixture in this directory is derived only from content already sanitized by Amaral Driver
Lab and published in the referenced Issue. Raw emulator logs must never be copied into
`src/test/resources`.

- `issue-43-sanitized.log`: sanitized excerpt published in Issue #43.
- `issue-39-sanitized.log`: sanitized excerpt published in Issue #39.
