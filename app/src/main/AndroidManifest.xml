<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="true"
        android:version="0x00400000" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DriverLab">

        <activity
            android:name=".TelemetryActivity"
            android:exported="false" />

        <activity
            android:name=".QualificationActivity"
            android:exported="false" />

        <activity
            android:name=".CampaignActivity"
            android:exported="false" />

        <activity
            android:name=".Phase4Activity"
            android:exported="false" />

        <activity
            android:name=".VisualRunnerActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:process=":runner"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.DriverLab.VisualRunner" />

        <activity
            android:name=".RunnerActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:process=":runner"
            android:theme="@style/Theme.DriverLab.Runner" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
