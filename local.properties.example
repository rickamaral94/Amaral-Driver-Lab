# Copy to local.properties and adjust.
sdk.dir=/path/to/Android/Sdk

# Optional: GitHub OAuth app client id used by the Device Flow publisher.
# There is no client secret in the APK by design (see docs/PRIVACY.md).
GITHUB_CLIENT_ID=
